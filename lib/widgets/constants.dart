import 'package:flutter/material.dart';

const Color correct = Colors.green;
const Color incorrect = Color.fromARGB(255, 255, 0, 0);
const Color neutral = Colors.white;
const Color background = Colors.pinkAccent;
